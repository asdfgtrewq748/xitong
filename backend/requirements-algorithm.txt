# 算法优化模块依赖
# 安装: pip install -r requirements-algorithm.txt

# 核心依赖(必需)
numpy>=1.21.0
pandas>=1.3.0
scipy>=1.7.0
scikit-learn>=0.24.0

# 可选依赖: 克里金插值
# 安装后可使用真正的克里金插值,否则使用高斯RBF近似
pykrige>=1.6.0

# 说明:
# - pykrige是可选的,如果不安装,系统会自动使用高斯RBF近似克里金插值
# - 安装pykrige可以获得更准确的插值结果和预测方差估计
# - 如果安装pykrige失败,系统仍可正常运行

# 验证安装:
# python -c "import pykrige; print('pykrige已安装')"
